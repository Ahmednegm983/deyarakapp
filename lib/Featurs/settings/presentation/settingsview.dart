import 'package:deyarakapp/Featurs/settings/presentation/widgets/settingswidget.dart';

import 'package:flutter/material.dart';

class settings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: settingswidget(),
    );
  }
}
